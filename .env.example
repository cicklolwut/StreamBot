# StreamBot Configuration Example
# Copy this file to .env and update with your values

# === DISCORD SELFBOT CONFIGURATION ===
# Your Discord user token (required)
TOKEN=""

# Command prefix (default: "$")
PREFIX="$"

# Discord server ID where the bot will operate
GUILD_ID=""

# Channel ID where the bot will respond to commands
COMMAND_CHANNEL_ID=""

# Voice/video channel ID where the bot will stream videos
VIDEO_CHANNEL_ID=""

# === GENERAL SETTINGS ===
# Local directory for storing video files (default: "./videos")
VIDEOS_DIR="./videos"

# Directory for caching video preview thumbnails (default: "./tmp/preview-cache")
PREVIEW_CACHE_DIR="./tmp/preview-cache"

# === STREAMING CONFIGURATION ===
# Whether to respect the original video parameters (true/false, default: false)
STREAM_RESPECT_VIDEO_PARAMS="false"

# Video stream resolution (default: 1280x720)
STREAM_WIDTH="1280"
STREAM_HEIGHT="720"

# Video stream frame rate (default: 30)
STREAM_FPS="30"

# Video bitrate in Kbps (default: 2000)
STREAM_BITRATE_KBPS="2000"

# Maximum video bitrate in Kbps (default: 2500)
STREAM_MAX_BITRATE_KBPS="2500"

# Enable hardware acceleration for video decoding (true/false, default: false)
STREAM_HARDWARE_ACCELERATION="false"

# Video codec to use (H264, H265, VP8, default: H264)
STREAM_VIDEO_CODEC="H264"

# H26x encoding preset (ultrafast, superfast, veryfast, faster, fast, medium, slow, slower, veryslow)
# Default: ultrafast (fastest encoding, larger file size)
STREAM_H26X_PRESET="ultrafast"

# === WEB SERVER CONFIGURATION (OPTIONAL) ===
# Enable the built-in web interface for video management (true/false, default: false)
SERVER_ENABLED="false"

# Web interface credentials (default: admin/admin)
SERVER_USERNAME="admin"
SERVER_PASSWORD="admin"

# Web server port (default: 8080)
SERVER_PORT="8080"

# === JELLYFIN INTEGRATION (OPTIONAL) ===
# Enable Jellyfin integration (true/false, default: false)
JELLYFIN_ENABLED="false"

# Jellyfin server URL (for the separate Jellyfin Discord bot)
JELLYFIN_BASE_URL="http://localhost:8096"

# Jellyfin API key (for the separate Jellyfin Discord bot)
# Generate this in Jellyfin Dashboard > API Keys
JELLYFIN_API_KEY=""

# Jellyfin user ID (optional, for user-specific libraries)
JELLYFIN_USER_ID=""

# Jellyfin library ID (optional, to restrict to specific library)
JELLYFIN_LIBRARY_ID=""

# === JELLYFIN DISCORD BOT API ===
# URL of the separate Jellyfin Discord bot API (default: http://localhost:3001)
JELLYFIN_BOT_API_URL="http://localhost:3001"

# Shared secret for authenticating with the Jellyfin Discord bot API
JELLYFIN_BOT_API_SECRET="your-secret-key"